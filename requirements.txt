torch
torchaudio
demucs
numpy<2
librosa
essentia
flask
flask-cors
scipy
soundfile
mutagen
waitress

# Multi-engine stem separation (Mel-Band Roformer Karaoke + MDX23C DrumSep)
audio-separator>=0.17.0
onnxruntime  # required by audio-separator; use onnxruntime-gpu instead for GPU inference

# Optional: HiFi++ GAN restoration for artifact removal
julius>=0.2.8

# Note: Madmom removed (doesn't install here -- pkg_resources incompatibility).
#       Essentia's RhythmExtractor2013 replaces it for all beat/BPM detection.
# Note: RubberBand is CLI tool, install via: apt-get/brew install rubberband-cli
#       Needed only for the optional "Align beatgrid" per-song feature.
